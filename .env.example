# Public domain (use localhost for local development)
DOMAIN=localhost

# Authgate base path
AUTH_BASE_PATH=/auth

# Internal service routing
AUTH_UPSTREAM=http://auth:8080
APP_UPSTREAM=http://app:80
